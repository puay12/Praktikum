import 'package:flutter/material.dart';

class NotifService extends StatelessWidget {
  const NotifService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
